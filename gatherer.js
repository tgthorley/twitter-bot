//
// Finds content to post
//
//Require Modules here - e.g. var Twit = require('twit');

var Gatherer = module.exports = function(config) {
  //this.twit = new Twit(config);
};

//
//Test Gatherer
//
Gatherer.prototype.test = function (callback) {
  console.log("testing gatherer")
};
